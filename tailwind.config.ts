import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      // Bạn có thể thêm các tùy chỉnh theme ở đây nếu cần
      // Ví dụ: colors: { 'custom-blue': '#1fb6ff' }
    },
  },
  plugins: [],
};

export default config;